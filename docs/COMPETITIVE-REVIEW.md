# 竞品评审与收敛决策

日期：2026-09-03 · 对应版本：v0.3.1

这份文档记录一次竞品评审的结论和由此确定的方向调整。它不描述现有功能（那在 [GUIDE.md](./GUIDE.md)），只回答两件事：**接下来不做什么，以及为什么。**

---

## 结论

Lexi 的功能面已经超过除沉浸式翻译外的全部竞品，**当前瓶颈是配置密度，不是功能缺口**。此后以收敛为主，新增能力必须能说明它替代掉了哪个旧配置项。

三条具体判断：

1. 「逐句精读侧栏」不做——竞品已经验证过并放弃了这个形态
2. 视频字幕不做——最贵的战场，且不是 Lexi 的差异化
3. 有一个安全默认值必须先改，优先级高于以上所有

---

## 一、竞品盘

| | 沉浸式翻译 | Trancy | Read Frog v1.46 | 流畅阅读 | **Lexi 0.3.1** |
|---|---|---|---|---|---|
| 定位 | 全形态翻译 | 视频语言学习 | 翻译 + 字幕 | 开源翻译 | **程序员语境学习** |
| 网页翻译 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 划词 + 术语解释 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 视频字幕 | ✓ 100+ 站 | ✓ 强项 | ✓ | ✗ | contract 已留，无 provider |
| 生词本 + SRS | ✗ | ✓ | ✓ Notebase | ✗ | ✓ 1→240 天 |
| PDF / EPUB / 漫画 | ✓ | PDF | ✗ | 图片 OCR | ✗ |
| **论坛速读** | ✗ | ✗ | ✗ | ✗ | **✓ 独有** |
| **GitHub Digest** | ✗ | ✗ | ✗ | ✗ | **✓ 独有** |
| **实体领域消歧** | ✗ | ✗ | ✗ | ✗ | **✓ 独有** |
| **中文页植入英文词** | ✗ | ✗ | ✗ | ✗ | **✓ 独有** |
| 页面问答 | ✗ | ✗ | ✗ | ✗ | ✓ |

四个独有能力集中在**技术阅读场景**，其余三家一个都没进来。这是 Lexi 唯一值得加固的护城河。

数据可信度：沉浸式翻译、流畅阅读来自官网抓取；Read Frog 来自源码核对；**Trancy 来自二手搜索结果，定价各来源说法不一（$2.69/30 天、$25.99/年、199 元/年），未经官网核实**；Language Reactor 官网抓取返回空，未列入对比。

---

## 二、不做逐句精读侧栏

### 竞品已经砍掉了它

Read Frog 官网截图里的「深入学习」侧栏（Sentence 1/2/3 + Key Words + Explanation + 水平档位 + Regenerate）在其 **v1.46.7 当前源码中已不存在**。

核对证据：

- `keyWords` / `articleAnalysis` / `readArticle` 全仓库 grep 零命中
- CHANGELOG PR #982：`remove read provider and unify per-feature provider model`
- CHANGELOG PR #1166：`Remove the deprecated Selection Toolbar AI button / Vocabulary Insight feature and keep Dictionary custom actions as the supported replacement`
- `src/entrypoints/sidepanel/` 现仅剩 `main.tsx`；`side.content/` 仅剩悬浮按钮

作者的重心已转向 `subtitles.content/`（29 个文件的完整字幕管线）、Notebase 生词本、Dictionary custom actions。做到 v1.46 才砍，这是投入换来的结论。

### 为什么它不成立

以下为推断，非作者公开说明：

- **成本按篇计**。整篇文章逐句生成 Key Words + Explanation，自带 key 的用户付不起
- **命中率低**。一篇文章真正卡住的可能只有 3 句，其余的生成是纯浪费
- **违反第一设计原则**。[PRODUCT.md](../PRODUCT.md) 写的是「留在阅读流里」；视线一旦移到右侧栏逐句读，就已经离开正文了

Lexi 现有的**划词卡片本身就是按需触发的逐句精读**，形态优于全文批量。

---

## 三、要抄的是水平档位，不是侧栏

截图顶部三个选择器 `English → Simplified Chinese → Intermediate`，前两个是语言，**第三个是水平档位**——解释颗粒度随用户水平变化。这是 Read Frog 做对且 Lexi 尚未做的唯一一件事。

对 Lexi 而言这不是新功能，是一个 prompt 参数：

- 现状：9 级学习等级只喂给了「网页词汇替换」
- 缺口：划词卡片的术语解释、实体检测的释义，都没有接入这个等级
- 落地：把等级注入这两处的 prompt。**零新增 UI，零新增配置项**

这一项的价值在于它换来了把「9 级 × 5 密度」那堆配置收掉的资格——等级一处定义，多处消费。

---

## 四、必须先改的安全默认值

**`同步中包含 API Key` 默认开启**（见 [GUIDE.md](./GUIDE.md) 隐私与数据一节）。

开启同步后，API Key 会随配置上传到浏览器厂商的同步服务器，当前只用文档提示用户自行关闭。

这不是配置项数量问题，是默认值把用户置于风险中。安全默认不应交给用户承担。

**改法**：默认关闭；用户主动开启时给一次明确的风险确认。

**优先级高于本文档其余全部事项。**

---

## 五、配置收敛清单

现状证据：`src/options/Options.vue` 2528 行，`src/contentScripts/pageEnhancer.ts` 5204 行。[PRODUCT.md](../PRODUCT.md) 的反参照写着「不做重型开发者面板」，实现走了反方向。

| 现在 | 收敛为 |
|---|---|
| 整页翻译 5 种触发方式 | 留 2 种：页面浮标 + 侧边栏。移除「无选区时 360ms 内连按两下 Ctrl/Alt」——误触温床 |
| 启动范围 4 选（仅本次 / 链接 / 站点 / Regex） | 2 选：仅本次 + 这个站点一直翻。Regex 收进高级设置 |
| 词汇替换 9 级 × 5 密度 × 3 显示方式 × 上限 1–80 | 3 档（轻 / 中 / 重）。密度与上限从学习等级自动推导 |
| 按域名 × 6 场景的开关矩阵 | 移除。保留全局开关 + 黑名单 |
| 限额 4 维度（日上限 / 滚动窗口 / 时段 / 每引擎） | 1 个「每日预算」 |
| 3 套速读卡片各 4 项配置 | 1 个开关，其余用默认值 |

判断依据：**每个配置项都是一次没有做出的产品判断。** 9 级 × 5 档 = 45 种组合，团队不可能全部验证，用户也不可能选对。

---

## 六、字幕：contract 保留，暂不投入

`src/logic/subtitles/`（commit 21c7ffa）的抽象设计是干净的——provider 分层、抛错等同于声明放弃并降级到下一源、AI 转录作为最低优先级且标记 billable。**保留，不动。**

暂不投入的理由：Read Frog 已在此投入 29 个文件的工程量并默认开启，Trancy 以此为付费卖点多年。这是最贵的战场，且与 Lexi 的技术阅读定位无关。

---

## 七、duo-translator 对照（2026-09-12）

[duo-translator](https://github.com/linuxscreen/duo-translator) 是另一类产品——通用浏览器翻译（WXT + React，v2.2.5，源码核对），不在上面的功能盘里比：它做网页/划词/输入框/YouTube 双语字幕/AI 工作台，接微软·谷歌·DeepL·Yandex·浏览器内置 AI 与自带 AI provider，同步走 Google Drive/WebDAV。对 Lexi 有参考价值的是**它怎么决定"这一页要不要翻"**。

**它的自动整页翻译默认开着。** `DEFAULT_VALUE` 里 `GLOBAL_SWITCH: true`、`DEFAULT_STRATEGY: 'auto'`，即访问任何非目标语言的页面都会自动整页翻译；网站级覆盖是 popup 里的两个开关（总是翻译此网站 / 不翻译此网站），**没有"站点规则自动恢复"这一层**。

**它把语言判断收成一层，Lexi 收成两层。** `main/strategy.ts` 的 `needsTranslate()` 是一个纯函数：全局开关 → 站点策略 → 默认策略 → `auto` 时比较**页面语言与目标语言**，页面已在目标语言、或语言在"不翻译语言"列表里就整页不翻。Lexi 的规则恢复路径此前没有这一步——规则只按 URL/站点/正则匹配，于是为英文文档保存的站点规则会连该站点的中文页面一起自动翻译。本次补上，并且比 duo 更细：

- **页面层**（对应 `needsTranslate`）：两条自动开始路径（保存规则恢复、双击修饰键）都先读页面语言；只采样**正文那一栏**（`main`/`article`/`role="main"` 内 ≥120 字符的块），站点导航/侧栏/页脚不参与——中文文章配英文站点导航是最常见的组合，而 duo 的页面采样会把它们一起算进去。目标语言在正文里占比 ≥60% 且语言判定置信度 ≥0.7 才判为"已是目标语言"。侧边栏会写明读到的语言，而不是只给一个跳过。
- **段落层**（对应 `main/content.ts:3751-3757` 的 `noTranslateLanguageFilter`）：duo 在派发前逐段过滤掉已是目标语言的段落；Lexi 现在是给每个候选块判语言（同一份判定按文档缓存，上限 400 块，滚动扫描不重复采样），是的的直接不生成翻译块——块 id 根本不会进入请求，而不是等 provider 返回后丢弃。混排页面因此只翻真正需要的那部分。

**双击修饰键的语义不同。** duo 也支持双击 Ctrl/Alt，但它只作用于划词 / 输入框 / 悬停段落（`handleDoubleTapModifier`），**不会整页翻译**；Lexi 在无选区时双击即整页翻译，且 GUIDE 标注为不可关闭——"误触一下就自动翻译整页"的来源之一是它，也是本文档待办 P2（移除双击修饰键）的动因。本次先给它补上同一道语言门禁并显示原因，不改变手势本身。

**抄与不抄：**

- **抄**：把"页面语言是否已是目标语言"作为自动翻译前的第一道判断（已落地，且只读正文那一栏 + 占比判定）。
- **抄**：派发前逐段过滤已是目标语言的段落（已落地，见上）。
- **不抄**：默认开启的全局自动整页翻译。它把成本与页面改动推给用户，和 Lexi"默认不动页面"的立场相反。
- **不抄**：域名 × 策略三态矩阵（总是/从不/自动），这正是本文档要收敛的配置密度。
- **记录**：不翻译语言的列表比 Lexi 现有的方向开关更省心——候选，不排期。

---

## 待办（按优先级）

- [ ] **P0** 关闭 API Key 参与同步的默认值，改为用户主动开启 + 风险确认
- [ ] **P1** 把学习等级注入划词卡片术语解释与实体检测释义的 prompt
- [ ] **P1** 词汇替换配置收敛为 3 档，密度与上限改为自动推导
- [ ] **P2** 整页翻译触发方式收敛为 2 种，移除双击修饰键
- [ ] **P2** 启动范围收敛为 2 选，Regex 移入高级
- [ ] **P2** 移除按域名 × 场景的开关矩阵
- [ ] **P3** 限额收敛为单一「每日预算」
- [ ] **P3** 速读卡片配置收敛为单开关
- [ ] **P3** 拆分 `pageEnhancer.ts`（5204 行）与 `Options.vue`（2528 行）

---

## 来源

- [沉浸式翻译](https://immersivetranslate.com/) · 官网抓取
- [流畅阅读](https://fluent.thinkstu.com/) · 官网抓取
- [read-frog 源码](https://github.com/mengxi-ream/read-frog) · v1.46.7 浅克隆核对
- [Trancy Chrome 商店](https://chromewebstore.google.com/detail/mjdbhokoopacimoekfgkcoogikbfgngb) · 二手来源，未核实
- [duo-translator 源码](https://github.com/linuxscreen/duo-translator) · v2.2.5 浅克隆核对（2026-09-12）
