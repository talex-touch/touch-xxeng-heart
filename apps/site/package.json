{
  "name": "@lexi/site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt generate",
    "generate": "nuxt generate",
    "preview": "pnpm run build && wrangler pages dev .output/public",
    "deploy": "pnpm run build && wrangler pages deploy .output/public --project-name=lexi-web"
  },
  "devDependencies": {
    "nuxt": "^4.5.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "wrangler": "4.118.0"
  }
}
